<!DOCTYPE html>

<html lang="en" xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta charset="utf-8" />
    <title>PA 5, 6, 7</title>
    <link rel="stylesheet" type="text/css" href="stylesheet.css">
</head>
<body>

    <h1>Overview</h1>

    <article id="ArticleOne">

    <p>Connor Kirby, Ben Wendell, Daniel Sanchez</p>
    <p>CS 460</p>
    <p>October 21, 2015</p>
    <br />
    
    <p>We used C to write the compiler for C.</p>
    <p>For the symbol table, we created a stack of BSTs. Each BST represents a scope of the program. Function definitions and global variables always go in the bottom BST.</p>
    <br />

    <p>For the length of each line, we set a hard limit of 500 characters. Any longer than this will crash our compiler.</p>
    <p>The compiler will quit after the first syntax error. It will then output the line with the syntax error to the console.</p>

    <br />
    <p>For the command line line arguments, there are options to send the output of each phase to a file. When running a.out, you must enter the input filename as one of the arguments. The second argument allows output of each phase to a file. The possible options are -d[lps] and any combonation of l, p, and s. Using -d[l] will save the scanner output to lexOutput.txt. Using -d[p] will save the parse output to parserOutput.txt. Using -d[s] will save the symbol table.</p>


</artice>

<nav>
    <ul>
        <li><a rel="Internal" href="./index.html">home</a></li>
    </ul>
</nav>

</body>
</html>
